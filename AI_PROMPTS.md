# AI 提问记录

> 记录范围：本仓库当前可获得的用户提示词。根据要求，不记录系统提示词、开发者提示词、平台内部指令、普通命令或工具参数；未伪造不可获得的历史记录。以下记录未发现密码、API Key、Cookie 或 Token。时间无法从消息可靠取得，故不填写。

## AP-001

- 所属阶段：仓库初始化
- 提问对象：Codex（本项目助手）
- 提示词原文：

~~~~text
将本项目关联到我的GitHub仓库，仓库地址：[https://github.com/ph-kleene/quant-research-onboarding](https://github.com/ph-kleene/quant-research-onboarding)
~~~~

## AP-002

- 所属阶段：仓库初始化
- 提问对象：Codex（本项目助手）
- 提示词原文：

~~~~text
再试一次推送
~~~~

## AP-003

- 所属阶段：仓库初始化
- 提问对象：Codex（本项目助手）
- 提示词原文：

~~~~text
使用SSH连接
~~~~

## AP-004

- 所属阶段：需求分析与方案设计
- 提问对象：Codex（本项目助手）
- 提示词原文：

~~~~text
你现在是本项目的产品负责人、量化研究负责人和解决方案架构师。

这是一个“量化策略实习生笔试”项目。当前阶段的唯一目标，是产出一套足够明确、能够直接指导后续实现的需求与设计文档。

这些文档将作为后续开发的唯一实施依据，因此不能停留在概念层面，不能保留会影响实现方向的歧义，也不能把关键决策留到开发阶段再处理。

# 一、首先读取项目现状

开始前必须完成以下只读检查：

1. 阅读项目根目录的 `REQUIREMENTS.md`；
2. 检查当前目录结构；
3. 检查 Git 状态和最近提交，了解仓库是否已有用户内容；
4. 不得修改 `REQUIREMENTS.md`；
5. 不得覆盖、删除或回滚任何已有文件和用户改动。

`REQUIREMENTS.md` 是原始笔试需求基线。

如果不同信息之间存在冲突，按以下优先级处理：

1. `REQUIREMENTS.md` 中标记为“原题要求”的内容；
2. 本提示词中的阶段目标和执行要求；
3. 你根据专业经验补充的设计建议；
4. 你的合理假设。

所有补充需求、假设和建议都必须明确标注来源，不得包装成招聘方的原始要求。

# 二、项目最终目标

最终成果需要同时满足四个目标：

## 1. 真正好用

目标读者是具备一定金融、编程或数据分析基础，但没有系统掌握量化多因子研究流程的新实习生。

最终成果应使读者能够：

- 建立量化研究的完整认知框架；
- 理解一项研究从问题提出到结果验证的完整闭环；
- 知道每一步应输入什么、输出什么、检查什么；
- 识别常见研究错误和不可信结果；
- 能够按照模板开始一项基础因子研究；
- 能够判断自己的研究是否具备继续推进的价值。

## 2. 有新意

最终成果不能只是量化知识点汇总、普通课程讲义或网上资料的重新整理。

创新必须解决真实问题，例如：

- 新人不知道如何把零散知识串成研究闭环；
- 会计算因子，但不知道如何判断结果是否可信；
- 回测结果很好，但不知道是否存在未来函数或数据偏差；
- 知道研究步骤，但无法独立开展第一项研究；
- 研究过程无法复现、审计或交接。

每个创新点必须写清楚：

- 它解决的具体问题；
- 用户如何使用；
- 为什么比普通文档更有效；
- 24 小时内是否能实现；
- 如何验证它确实有价值；
- 实现失败时的降级方案。

不得为了视觉效果或技术炫技加入没有明确用户价值的功能。

## 3. 展示工程能力

设计应体现以下工程思考：

- 清晰的模块边界；
- 数据流和数据契约；
- 配置管理；
- 数据缓存和离线降级；
- 确定性与随机种子；
- 研究结果复现；
- 自动化测试；
- 数据质量检查；
- 内容正确性检查；
- 构建和交付流程；
- 错误处理；
- 可维护性；
- 可扩展性；
- 第三方数据和依赖风险；
- 许可证、引用和数据来源管理。

这些能力必须服务于最终成果，不能设计成超出笔试规模的大型系统。

## 4. 展示研究思维

设计必须体现：

- 研究问题如何形成；
- 研究假设如何表达；
- 因子的经济逻辑；
- 数据和样本如何选择；
- 证据如何支持或否定假设；
- 如何区分探索性结论和确认性结论；
- 如何防止未来函数、幸存者偏差、数据窥探和过拟合；
- 如何进行样本外验证和稳健性检验；
- 如何理解交易成本、换手率、容量和风险暴露；
- 如何记录失败实验；
- 如何判断研究结果是否值得进入下一阶段。

# 三、当前阶段的严格边界

当前阶段只进行需求分析和方案设计。

允许：

- 创建或修改 Markdown 文档；
- 查阅可靠资料；
- 使用 Mermaid 绘制架构图、流程图或数据流图；
- 编写伪代码、接口定义示例和数据结构示意，但不得形成可执行实现；
- 在文档中规划未来目录结构。

禁止：

- 编写正式源代码；
- 创建网页、应用、Notebook 或数据处理脚本；
- 下载或生成正式数据集；
- 实现最终学习资料；
- 安装依赖；
- 修改项目配置；
- 修改 `REQUIREMENTS.md`；
- 提交 Git；
- 推送 GitHub；
- 删除、覆盖或回滚已有内容。

本阶段结束后，项目中新增内容只能是 Markdown 设计文档。

# 四、必须完成的方案决策

你可以研究多个方案，但最终文档中必须选择且只能选择一个主方案。

不得以“可以使用 A 或 B”“后续根据情况决定”等方式回避关键决策。

至少比较三种方案：

1. 低风险方案：以结构化学习手册为核心；
2. 平衡方案：文档、交互体验与可复现实验相结合；
3. 进取方案：更完整的交互式量化研究训练产品。

可以调整具体形式，但三种方案必须在复杂度和价值上形成明显梯度。

使用加权决策矩阵进行评估，权重合计必须为 100%。至少包含以下维度：

- 新人学习效果；
- 量化研究流程覆盖度；
- 实用性；
- 新颖性；
- 工程能力展示效果；
- 研究严谨性；
- 24 小时内完成的可行性；
- 演示和提交便利性；
- 外部依赖风险；
- 失败后的可降级能力。

最终必须锁定以下决策：

- 最终产品形态；
- 主要使用方式；
- 核心用户路径；
- 核心案例；
- 案例使用的资产类别、市场、频率和数据范围；
- 数据来源策略；
- 是否需要联网；
- 离线时如何运行或展示；
- 技术栈；
- 交付和演示方式；
- MVP 范围；
- 加分项范围；
- 明确不做的内容；
- 时间不足时的降级版本。

任何会影响后续实现的关键字段都不能只写 `TBD`。

如果信息不足，应选择一个合理方案，记录为“设计假设”，并解释选择依据和替代方案。

# 五、内容与学习体验要求

设计的学习内容必须覆盖完整的量化多因子研究闭环，而不是只介绍因子计算或回测。

至少包括：

1. 研究问题定义；
2. 假设和经济逻辑；
3. 数据获取与数据字典；
4. 数据清洗和质量检查；
5. 可交易股票池或研究样本构建；
6. 因子计算；
7. 缺失值、异常值、标准化和中性化处理；
8. 因子有效性检验；
9. IC、分组收益、稳定性和衰减分析；
10. 多因子组合；
11. 组合构建；
12. 回测；
13. 交易成本和换手率；
14. 风险暴露；
15. 样本外测试；
16. 稳健性检验；
17. 失败实验和结果解释；
18. 研究结论；
19. 研究复现；
20. 从研究到上线或下一阶段评审的条件。

必须设计一个贯穿始终的案例。该案例不能只用于演示最终结果，而应从研究问题开始，贯穿数据、因子、验证、组合、回测、失败诊断和研究结论。

还应设计：

- 新人学习路径；
- 快速浏览路径；
- 深入实践路径；
- 研究步骤检查清单；
- 常见错误与修正对照；
- 研究结果可信度检查机制；
- 学习完成后的可衡量成果。

# 六、必须创建的文档

统一使用中文撰写，技术名词可以保留英文。文件名使用以下固定名称。

## 1. `docs/00-executive-summary.md`

必须包含：

- 项目一句话定义；
- 最终推荐方案；
- 核心用户价值；
- 最重要的创新点；
- 最终计划交付物；
- MVP；
- 加分项；
- 关键技术决策；
- 最大风险；
- 降级方案；
- 24 小时整体安排。

这份文档必须能让评审者在几分钟内理解项目将做成什么。

## 2. `docs/01-product-requirements.md`

必须包含：

- 背景；
- 原题硬性要求；
- 用户画像；
- 用户痛点；
- 项目目标；
- 可衡量的学习目标；
- 功能需求；
- 非功能需求；
- 内容范围；
- 明确不做的内容；
- 用户使用场景；
- 用户路径；
- 交付要求；
- 验收标准；
- 设计假设；
- 约束；
- HR 待确认问题。

每一项需求必须有唯一编号，例如 `FR-001`、`NFR-001`，供后续追踪。

## 3. `docs/02-solution-options-and-decision.md`

必须包含：

- 三种候选方案的完整描述；
- 每种方案的用户体验；
- 每种方案的技术形态；
- 每种方案的优点、缺点和风险；
- 权重合计为 100% 的决策矩阵；
- 评分依据；
- 最终只选择一个主方案；
- 选择该方案的理由；
- 没有选择另外两种方案的原因；
- 主方案的 MVP；
- 主方案的加分项；
- 降级方案及其触发条件。

## 4. `docs/03-learning-and-content-design.md`

必须包含：

- 整体信息架构；
- 学习路径；
- 内容目录；
- 各章节学习目标；
- 各章节输入与输出；
- 贯穿案例的完整设计；
- 交互或练习设计；
- 错误案例设计；
- 研究质量检查点；
- 新人可复用的模板与清单；
- 如何验证读者确实获得了能力提升；
- 内容引用和事实核验方式。

不得只列章节标题，必须说明每一部分为什么存在、如何使用，以及与前后步骤的关系。

## 5. `docs/04-technical-design.md`

必须包含：

- 系统上下文；
- 整体架构；
- 模块边界；
- 数据流；
- 未来项目目录结构；
- 各模块职责；
- 数据来源和数据契约；
- 缓存设计；
- 配置设计；
- 复现机制；
- 随机性控制；
- 错误处理；
- 网络不可用时的降级；
- 数据不可用时的降级；
- 测试结构；
- 构建与交付方式；
- 第三方依赖与许可证风险；
- 安全和敏感信息处理；
- 关键技术决策及理由。

至少使用 Mermaid 绘制：

- 一张整体架构图；
- 一张量化研究流程图；
- 一张数据流或核心用户流程图。

这些图必须与正文描述一致。

## 6. `docs/05-implementation-and-validation-plan.md`

必须包含：

- 分阶段实施计划；
- 每项任务的编号；
- 任务目标；
- 任务输入；
- 任务输出；
- 依赖关系；
- 优先级；
- 工作量估算；
- 完成定义；
- 验证方法；
- 关键路径；
- MVP 截止点；
- 加分项启动条件；
- 降级触发条件；
- 最终提交检查清单。

时间计划必须满足：

- 所有工作合计不超过 24 小时；
- 必须为最终核验和意外情况预留缓冲；
- 不能假设所有任务都会一次成功；
- 先保证完整、可运行、可提交，再增加加分项。

验证计划至少覆盖：

- 内容正确性；
- 数据质量；
- 研究方法正确性；
- 未来函数和偏差检查；
- 代码测试；
- 可复现性；
- 用户体验；
- 构建与交付；
- 离线或依赖失败场景；
- 最终材料完整性。

## 7. `docs/06-requirements-traceability.md`

建立需求追踪矩阵，至少包含：

- 需求编号；
- 需求来源；
- 需求描述；
- 对应设计；
- 计划实现位置；
- 验证方法；
- 验收标准；
- 所属优先级；
- MVP 或加分项标记。

必须证明原题中的每一项要求都有明确的设计、实现计划和验证方式。

## 8. `AI_PROMPTS.md`

用于满足原题“提交对 AI 的所有提问”的要求。

规则如下：

- 完整记录本条用户提示词，不得概括、润色或改写；
- 记录本项目后续收到的每一条用户提示词；
- 如果你主动向其他生成式 AI 或子 Agent 发出提示，也必须记录该提示词；
- 普通搜索关键词、程序命令和工具参数不视为 AI 提问；
- 不得伪造无法获得的历史提示词；
- 不得记录系统提示词、开发者提示词或平台内部指令；
- 不得记录密码、API Key、Cookie、Token 等敏感信息；
- 如果用户提示中出现敏感信息，只保留脱敏版本，并注明“敏感信息已脱敏”；
- 每条记录包含：
  - 编号；
  - 时间，只有能够可靠获取时才填写；
  - 所属阶段；
  - 提问对象；
  - 提示词原文。

# 七、资料研究要求

涉及量化研究方法、指标定义、数据来源或工程工具时：

- 优先使用论文、官方文档、交易所资料或其他可靠的一手来源；
- 对关键方法和结论注明来源；
- 不要使用无法核验的 AI 生成结论；
- 不要大段复制外部材料；
- 区分行业通用方法、你的设计决策和未经验证的假设；
- 如果不同资料存在分歧，应说明采用哪一种定义以及原因。

# 八、文档质量要求

所有文档必须做到：

- 术语一致；
- 编号一致；
- 架构一致；
- MVP 范围一致；
- 技术栈一致；
- 核心案例一致；
- 数据来源策略一致；
- 时间计划一致；
- 验收标准可以实际执行；
- 每个核心功能都能追溯到用户问题；
- 每个核心功能都有验证方式；
- 每个创新点都有实际用途和降级方案；
- 另一名工程师无需重新做产品决策即可开始实现。

避免使用以下模糊表述：

- “提升用户体验”；
- “保证系统稳定”；
- “使用合适的数据”；
- “后续再决定”；
- “可以选择 A 或 B”；
- “视情况而定”。

如果必须使用类似表述，应立即给出明确条件、指标、默认选择和触发规则。

# 九、HR 问题处理

将可能向 HR 提出的问题分为：

1. 阻塞性问题：答案不同会改变产品方向；
2. 非阻塞性问题：可以使用默认假设继续推进。

每个问题必须包含：

- 为什么需要询问；
- 不同答案会造成什么影响；
- 在未获得回复时采用什么默认假设。

除非确实存在无法通过合理假设解决的阻塞问题，否则不要停止工作等待用户回复。

# 十、完成前的强制自检

完成文档后，逐项检查：

1. 是否覆盖 `REQUIREMENTS.md` 中的所有原题要求；
2. 是否明确选择了一个且只有一个主方案；
3. 是否锁定了技术栈、案例、数据策略、交付形态和 MVP；
4. 是否仍有影响实现的 `TBD`；
5. 是否能在 24 小时内完成；
6. 是否预留了缓冲时间；
7. 是否存在为了炫技而没有用户价值的功能；
8. 是否完整体现量化研究闭环；
9. 是否有未来函数、数据偏差和过拟合防范设计；
10. 是否有可复现和离线降级方案；
11. 是否每项需求都有实现位置和验证方法；
12. Mermaid 图与正文是否一致；
13. 所有文档之间是否存在冲突；
14. `AI_PROMPTS.md` 是否完整记录了本条提示词；
15. 当前阶段是否只新增或修改了 Markdown 文档；
16. 是否没有提交或推送 Git。

如检查发现问题，应先修改文档，再进行最终汇报。

# 十一、最终回复格式

完成后只需简要汇报：

1. 最终推荐方案；
2. 推荐方案的核心用户价值；
3. 最有价值的创新点；
4. MVP 范围；
5. 加分项；
6. 核心技术决策；
7. 主要风险和降级方案；
8. 尚待用户决定的问题；
9. 创建或修改的文档列表；
10. 明确说明当前尚未开始实现代码。

不要在最终回复中重复整套文档内容。
~~~~

## AP-005

- 所属阶段：实施前定稿
- 提问对象：Codex（本项目助手）
- 提示词原文：

~~~~text
请对当前项目的设计文档进行最后一次“实施前定稿”。

本轮仍然只修订 Markdown 文档，不开始正式实现，不安装依赖、不下载数据、不创建代码或配置、不提交或推送 Git。

必须完整阅读：

- REQUIREMENTS.md
- docs/00-executive-summary.md
- docs/01-product-requirements.md
- docs/02-solution-options-and-decision.md
- docs/03-learning-and-content-design.md
- docs/04-technical-design.md
- docs/05-implementation-and-validation-plan.md
- docs/06-requirements-traceability.md
- AI_PROMPTS.md

本轮以以下用户确认信息为准。

# 一、用户已经确认的实施条件

## 1. 时间

用户明确要求：

- 不需要因为时间限制主动削弱方案；
- 可以充分利用 AI 完成内容、代码、测试与工程工作；
- 仍然需要有实施优先级和降级能力，但不得仅以“24 小时可能来不及”为理由删除有价值的核心设计；
- 不需要继续追问收题时间或截止时间。

因此：

- 保留 P0/P1 和关键路径，用于控制实施顺序与失败风险；
- 删除“剩余 18 小时”“必须占剩余时间 75%”等未经用户确认的时间假设；
- 可以保留工作量估算，但其作用是规划顺序，不是主动压缩产品价值；
- 24 小时仍作为原题背景约束记录，但不需要在本轮继续要求用户提供时间信息；
- 不得因为时间原因将真实数据、三因子案例、REG、证据护照或失败案例降级。

## 2. 安装权限

用户允许安装任何完成项目所必需的本地依赖，只要最终能在本机运行。

当前 WSL 已知状态：

- 系统 Python 为 3.14.4；
- 当前未发现 uv；
- 当前未发现 Quarto；
- 当前未发现可用于自动打印 PDF 的 Chromium/Chrome；
- 当前未设置 TUSHARE_TOKEN。

设计中必须锁定：

1. 使用 uv 管理项目环境；
2. 使用 uv 安装并固定 CPython 3.12，不直接依赖系统 Python 3.14.4；
3. 安装 Quarto；
4. 安装项目需要的 Python 包、构建工具和测试工具；
5. 如 PDF 最终确有价值，可以安装浏览器、TinyTeX 或其他必要工具；
6. 优先用户级安装；必须使用 sudo 时允许安装，但应限制在本项目必需依赖；
7. 安装完成后记录版本并进入证据护照；
8. 提供一条统一的环境初始化命令或脚本；
9. 提供一条统一的测试命令；
10. 提供一条统一的构建命令；
11. 提供一条统一的本地预览命令。

不需要再把“Quarto 不存在”视为产品降级理由，因为用户已经允许安装。

但仍需保留构建失败降级：

- GitHub Pages 失败时，本地 HTML 仍可交付；
- PDF 失败时，HTML 不受影响；
- 在线数据刷新失败时，冻结站点和已有合规缓存仍可使用。

## 3. Tushare 权限

用户拥有 Tushare Pro：

- 实际积分：2120；
- 调用频率：200 次/分钟；
- 截图显示其账户属于 2000 积分档；
- 可见权限类别包括：
  - 基础数据；
  - 低频行情（日、周、月）；
  - 财务数据；
  - 宏观经济；
- 用户可以在实施阶段提供 Token。

因此必须修改为：

- 真实 Tushare 数据是主实现路线；
- 确定性教学数据仅作为测试夹具、离线演示和接口失败降级；
- 不再把教学数据写成默认主路线；
- 不得在没有实际探测前假定每个接口一定有权限；
- 实施第一阶段必须对所需 endpoint 做最小化能力探测；
- 单个 endpoint 不可用时，只降级对应能力，不要立即放弃整个真实数据案例。

实施前能力探测至少覆盖：

- trade_cal；
- index_basic；
- index_weight；
- daily；
- adj_factor；
- daily_basic；
- stk_limit；
- suspend_d；
- index_daily。

每个接口探测只请求最小日期区间和最少字段，并记录：

- 是否成功；
- HTTP/API 状态；
- 返回字段；
- 返回行数；
- 权限错误；
- 实际接口代码；
- 是否满足历史区间；
- 是否满足当前设计用途。

不得在日志中输出 Token。

调用层必须按 200 次/分钟设计：

- 默认限速不得超过 180 次/分钟，保留安全余量；
- 遇到 429 时按服务端反馈或指数退避；
- 已成功响应必须进入不可变缓存；
- 重试不得重复写入不同内容的同一 snapshot；
- 优先选择能减少请求次数的日期批量或证券批量方式；
- 下载过程必须可恢复，不得因一次失败从头开始；
- 全量取数前先估算请求数量；
- manifest 记录 endpoint、参数、字段、行数、数据区间、哈希和取得时间。

## 4. Token 的安全注入

Token 不得直接发送到项目聊天中，因为本项目需要提交全部 AI 提问记录。

文档必须规定：

- Token 通过 WSL 环境变量 `TUSHARE_TOKEN` 或仓库外的本地秘密文件注入；
- 如果使用秘密文件，其位置固定为：
  `~/.config/shangchen-quant-research-onboarding/tushare.env`
- 文件权限必须为 `600`；
- 项目只读取，不复制该文件；
- 不得把 Token 写入 `.env.example`；
- `.env.example` 只能写变量名和说明；
- 不得把 Token 写入 Notebook、日志、异常、测试快照、HTML、PDF、缓存 manifest 或证据护照；
- AI_PROMPTS.md 对任何意外出现的 Token 必须脱敏；
- 项目从第一天开始就配置 `.gitignore`，而不是最后才处理；
- 最终公开前仍要进行一次完整脱敏审计。

如果 Token 曾经进入 Git 历史：

- 仅删除当前文件不算完成；
- 必须停止推送；
- 提醒用户轮换 Token；
- 清理 Git 历史；
- 重新进行密钥扫描后才能公开。

# 二、最终交付方式

用户确认：

- GitHub 仓库是公开的；
- 最终可以直接交付项目地址；
- 学习资料可以采用任何形式；
- 核心要求是最终成果确实能够被使用；
- HR 没有额外要求；
- 不需要继续向 HR 提问；
- 没有外部试用者；
- 用户本人可以承担最终试用。

因此锁定为：

## 主交付

1. 公开 GitHub 仓库；
2. GitHub Pages 上可直接访问的中文量化研究入门学习站；
3. 本地可离线打开的静态 HTML；
4. 可执行的端到端 Notebook；
5. 完整源码、测试、构建说明和复现说明；
6. 研究模板、REG、证据护照和失败实验；
7. AI_PROMPTS.md。

## 备份交付

- GitHub Pages 构建失败时，仓库仍提供本地构建和离线 HTML；
- PDF 是便利性输出，不是核心产品成立的必要条件；
- 如果 PDF 能稳定生成，则提供；
- 如果 PDF 影响主要站点质量，不得为 PDF 牺牲 HTML、Notebook 或研究正确性。

项目首页 README 必须做到：

- 一眼说明这是什么；
- 提供在线学习站入口；
- 提供本地运行入口；
- 提供 30 分钟、3 小时和深入实践三条入口；
- 明确数据权限；
- 明确无 Token 时能看到什么；
- 明确有 Token 时如何刷新案例；
- 明确不是投资建议；
- 提供最终成果截图或预览。

# 三、保留并强化的方案

必须保留：

1. 中文 Quarto 静态学习站；
2. 沪深300三因子贯穿案例；
3. 20 步完整研究地图；
4. 三条学习路径；
5. REG 六道研究可信度门禁；
6. 研究证据护照；
7. 正确/失败双轨案例；
8. 可复现研究流水线；
9. 数据、时序、成本和样本外验证；
10. 自动化测试；
11. GitHub Pages；
12. 离线降级；
13. AI 提问记录。

可以实施完整方案，但必须避免为了展示工程能力而加入与学习目标无关的复杂后端、数据库、登录、任务队列或在线交易功能。

“新颖”和“工程化”仍必须服务于新人学习量化研究。

# 四、修正数据接口与交易规则

必须建立正式的数据能力矩阵，包含：

- endpoint；
- 所需字段；
- 预计权限；
- 官方更新时间；
- P0/P1；
- 调用方式；
- 预计请求量；
- 缓存键；
- 研究用途；
- 不可用时的唯一降级方案。

至少覆盖：

- trade_cal；
- index_basic；
- index_weight；
- daily；
- adj_factor；
- daily_basic；
- stk_limit；
- suspend_d；
- index_daily。

必须明确：

1. `daily_basic.limit_status` 是收盘状态，不能单独决定次日开盘订单能否成交；
2. `stk_limit` 提供每日涨跌停价格；
3. `suspend_d` 提供停复牌信息；
4. 日线缺失可以作为停牌或无行情的辅助判断，但不得与正式停牌信息混为一谈；
5. 次日开盘执行采用以下保守规则：
   - 没有有效日线或全日停牌：不成交；
   - 买入且开盘价达到涨停价：不成交；
   - 卖出且开盘价达到跌停价：不成交；
   - 卖出遇涨停、买入遇跌停不得自动判为不可成交；
   - 其他具有开盘成交量的情况按开盘价成交；
6. 上述是日频数据下的教学近似，无法模拟排队、盘口深度和部分成交；
7. 未成交订单保留原持仓，并在后续首个满足规则的交易日重试；
8. 延迟成交次数、延迟天数和受影响权重必须进入报告。

# 五、修正时间字段

源接口通常不直接提供严格意义上的 `available_at`。

不得把 `available_at` 写成所有源表天然存在的字段。

统一改为：

- `trade_date`：市场数据所属日期；
- `effective_date`：成分或规则生效日期；
- `ingested_at`：本项目取得数据的时间；
- `usable_from`：根据官方更新时间和保守规则推导出的最早可使用时间；
- `signal_at`：信号真正形成时间；
- `execution_at`：交易假设发生时间。

必须满足：

`usable_from <= signal_at < execution_at`

月末信号不得描述为“收盘瞬间形成”。

默认定义为：

- 月末交易日收盘后；
- 等待 daily、daily_basic、index_daily 等 P0 数据按官方更新窗口完成；
- 在当日晚间固定时间生成信号；
- 下一交易日开盘执行。

每个 endpoint 的 `usable_from` 推导规则必须在数据能力矩阵中明确。

无法证明历史公告时点的数据必须披露限制，不得制造虚假的 point-in-time 精度。

# 六、修正复权、标签和收益计算

技术设计必须锁定：

1. 股票原始 OHLC 使用 `daily`；
2. 复权因子使用 `adj_factor`；
3. 动量和持有期收益使用相邻价格与对应复权因子之比；
4. 不依赖未来最终复权因子归一化后的绝对价格水平；
5. 示例公式：
   `adjusted_return = (price_t × adj_factor_t) / (price_t-1 × adj_factor_t-1) - 1`
6. 月度因子标签必须严格后移；
7. 信号、执行、持有和评价区间必须有统一时钟图；
8. 组合收益口径必须固定；
9. 成本在实际换仓发生时扣除；
10. 未成交仓位继续产生持有收益；
11. 因涨跌停或停牌延后的订单只在实际假设成交时计入换手和成本；
12. 基准收益必须与组合评价区间对齐。

# 七、修正基准设计

H00300 确实是中证指数公司公布的沪深300全收益指数代码，但当前尚未证明用户的 Tushare 权限和 `index_daily` 能直接返回该代码。

设计必须写成：

1. 先调用 `index_basic` 获取可用的完整 `ts_code`；
2. 对 H00300 候选代码做最小区间 `index_daily` 探测；
3. 成功后才锁定其为主基准；
4. 不得把裸 `H00300` 直接写进正式 API 调用；
5. 如果 H00300 在当前账户不可用：
   - 使用 Tushare 实际可返回的沪深300价格指数，如 `000300.SH` 或 `index_basic` 返回的对应代码；
   - 页面明确标注“价格指数基准”；
   - 不得称为全收益指数；
   - 不把相对价格指数的超额收益作为 REG 唯一硬门；
6. 如果后续找到合规、稳定、可复现的全收益指数来源，可以替换适配器，但不得影响主体实现；
7. 数据源、实际代码、数据区间和是否含分红必须写入证据护照。

# 八、样本期

将主案例样本期统一为：

- 总样本：2018-01-01 至 2025-12-31；
- 探索期：2018-01-01 至 2022-12-31；
- 确认期：2023-01-01 至 2025-12-31；
- 不使用尚未结束的 2026 年作为完整年度确认样本。

所有文档、配置规划、图表规划、验收标准和追踪矩阵必须同步更新。

# 九、修正确认集定义

删除“确认集只能运行一次”“确认运行次数必须等于 1”的字面规则。

统一定义为：

- 假设、因子定义、参数和评价规则在首次查看确认集结果前冻结；
- 只允许一次研究决策性揭盲；
- 相同代码、数据和配置的确定性复现不算新的研究试验；
- 工程错误修复后的重跑必须记录；
- 查看确认结果后再修改研究参数，确认集即被污染；
- 被污染的结果降级为探索性证据；
- 不得通过删除实验记录重新获得“未查看”状态。

证据护照记录：

- confirmation_reveal_count；
- research_decision_count；
- reproduction_run_count；
- contaminated；
- contamination_reason。

# 十、修正用户验证

用户确认没有外部试用者，由用户本人完成最终试用。

因此：

## P0 验收

必须能由项目 Agent 和用户独立完成：

- 自动测试；
- 离线构建；
- Notebook 端到端运行；
- 正确案例通过对应门禁；
- 失败案例触发预期门禁；
- 链接检查；
- 视觉检查；
- README 冷启动测试；
- 用户自测清单；
- 最终项目地址可访问。

## 用户自测

必须提供一份明确的用户自测脚本，包括：

1. 从 GitHub 项目首页进入；
2. 完成 30 分钟路径；
3. 运行或查看贯穿案例；
4. 找出失败案例问题；
5. 使用 REG 判断继续、补证或停止；
6. 使用研究模板创建一个新研究问题；
7. 记录理解障碍、运行障碍和内容错误；
8. 给出是否可以直接用于新人培训的结论。

三名代表用户、第二人换机复现和正式用户研究保留为未来产品指标，不作为本次提交硬门。

# 十一、REG 的最终定义

保留六道门：

- D：数据；
- T：时序；
- S：统计；
- C：成本；
- R：稳健性；
- P：复现。

必须区分两类结论：

## 研究有效性

数据、时序或复现出现红灯时，研究结论无效。

## 策略推进价值

统计、成本或稳健性不通过时，可以得到可信的“补证”或“停止”结论。

不得以策略必须盈利作为项目成功条件。

一个结果为负但过程可信、证据完整、结论克制的案例，仍然是成功的量化研究教学案例。

所有阈值必须标记为：

- 方法必需条件；
- 本项目教学决策；
- 或团队未来可配置条件。

不得把项目自定阈值冒充行业统一标准。

# 十二、GitHub 公开与最终脱敏

GitHub 仓库是公开的。

设计必须从实现第一天就避免秘密进入仓库，同时在最终提交前安排集中脱敏审计。

最终脱敏审计至少包括：

- Git tracked files；
- Git 未跟踪文件；
- Git 历史；
- Notebook 输入和输出；
- `_site`；
- PDF；
- 测试快照；
- 日志；
- 缓存 manifest；
- 异常消息；
- README 示例；
- AI_PROMPTS.md；
- `.env` 和配置文件；
- 高熵字符串；
- 常见 Token/API Key/私钥前缀；
- 本机用户名和不应公开的绝对路径；
- Tushare 原始响应和授权不明数据。

允许公开：

- 源代码；
- 学习内容；
- 自制图表；
- 汇总指标；
- 小型确定性教学夹具；
- 不可逆的数据哈希；
- 数据字段说明；
- 复现步骤。

默认禁止公开：

- Tushare Token；
- Cookie；
- 私钥；
- `.env`；
- Tushare 原始缓存；
- 能够大规模还原原始付费数据的导出；
- 带秘密的 Git 历史；
- 未确认再分发权限的数据。

派生图表和汇总指标在最终审计时判断；如无法确认再分发边界，应只公开教学夹具生成的可执行结果，并对真实市场结果提供不可逆汇总和方法说明。

# 十三、文档修改范围

同步修改：

- docs/00-executive-summary.md
- docs/01-product-requirements.md
- docs/02-solution-options-and-decision.md
- docs/03-learning-and-content-design.md
- docs/04-technical-design.md
- docs/05-implementation-and-validation-plan.md
- docs/06-requirements-traceability.md

同时：

- 将本条提示词原文追加到 AI_PROMPTS.md；
- 不修改 REQUIREMENTS.md；
- 不删除已有 AI 提问；
- 不新增非 Markdown 文件；
- 不安装依赖；
- 不调用 Tushare；
- 不开始实现；
- 不提交或推送 Git。

# 十四、最终一致性检查

修订完成后必须确认：

1. 真实 Tushare 数据是主路线；
2. 教学数据只是测试和降级；
3. 调用速率按不超过 180 次/分钟设计；
4. 全量下载前有最小接口探测；
5. Token 不会进入聊天、仓库或交付物；
6. 数据接口、字段和交易规则完全闭合；
7. H00300 不再被未经验证地假定可用；
8. 复权和收益公式明确；
9. 样本更新至 2025；
10. 确认集规则区分揭盲、研究决策和确定性复现；
11. GitHub Pages 是目标交付，但本地 HTML 是可靠备份；
12. PDF 不再阻塞核心交付；
13. 用户本人可以按自测脚本完成验收；
14. 不再依赖 HR、三名试用者或第二名工程师；
15. 所有设计文档和追踪矩阵保持一致；
16. 当前仍未开始实现代码。

# 十五、最终回复

完成后简要汇报：

1. 根据用户实际条件做了哪些调整；
2. 最终主交付物；
3. 最终真实数据路线；
4. Tushare 接口探测计划；
5. Token 安全方案；
6. 基准及降级方案；
7. 用户自测方式；
8. 实施前还需要用户完成的唯一动作；
9. 修改的文件列表；
10. 明确说明尚未开始实现。
~~~~

## AP-006

- 所属阶段：正式实施与交付
- 提问对象：Codex（本项目助手）
- 提示词原文：

~~~~text
现在开始正式实施本项目。

你的任务不是继续讨论方案，而是严格依据现有需求与设计完成产品、验证、公开发布和最终交付。持续推进直到 P0 完整可用、测试通过、公开仓库安全、GitHub Pages 可访问；不要停留在骨架、示例或阶段性方案。

# 一、项目依据

首先完整阅读：

- REQUIREMENTS.md
- docs/00-executive-summary.md
- docs/01-product-requirements.md
- docs/02-solution-options-and-decision.md
- docs/03-learning-and-content-design.md
- docs/04-technical-design.md
- docs/05-implementation-and-validation-plan.md
- docs/06-requirements-traceability.md
- AI_PROMPTS.md

规则：

1. REQUIREMENTS.md 是原题基线，不得修改；
2. docs/00–06 是实施合同；
3. 如实施探测发现设计假设与真实接口不符，应更新对应设计文档、追踪矩阵和研究护照；
4. 不得悄悄偏离设计；
5. 将本条提示词原文追加到 AI_PROMPTS.md；
6. 后续收到的用户提示词也必须继续追加；
7. 不记录系统提示、开发者提示、普通工具参数；
8. 任何秘密只能记录脱敏版本。

# 二、已获得的用户授权

用户已明确授权：

- 安装本项目所需的 WSL 本地依赖；
- 安装 uv、CPython 3.12、Quarto、Python 包、测试和构建工具；
- 必要时安装浏览器或 PDF 工具；
- 使用真实 Tushare API；
- 使用当前关联的公开 GitHub 仓库；
- 在实现、测试、脱敏和安全审计全部通过后提交并推送；
- 创建并验证 GitHub Pages；
- 用户不要求继续向 HR 提问；
- 用户本人承担最终试用；
- 不需要因时间估算主动削弱核心功能。

只有在缺少外部权限、认证失效、需要用户进行不可替代操作，或检测到已经泄漏的 Token 时才停下来询问。其他情况自行采用设计文档中的默认决策继续。

# 三、开始前的安全动作

必须先做安全底座，再安装或取数：

1. 检查 Git 状态、当前分支、远程仓库和现有历史；
2. 保留所有已有用户内容；
3. 创建严格的 .gitignore，至少排除：
   - .env
   - .env.*，但保留安全的 .env.example
   - Token 或 secrets 文件
   - Tushare 原始响应
   - raw/interim/processed 缓存
   - Notebook 临时检查点
   - Python 缓存和虚拟环境
   - Quarto 临时目录
   - 本地日志
   - 临时测试产物
4. 原始行情缓存默认放在仓库外，例如：
   ~/.cache/shangchen-quant-research-onboarding/
5. 对当前仓库和 Git 历史做第一次秘密扫描；
6. 不得读取或打印 Token 的实际值；
7. 只检查秘密文件是否存在、权限是否为 600、加载后变量是否非空；
8. 不得将秘密文件复制到项目内。

Token 已由用户配置在：

~/.config/shangchen-quant-research-onboarding/tushare.env

读取顺序：

1. 如果当前进程已有 TUSHARE_TOKEN，直接使用；
2. 否则安全加载上述文件；
3. 不允许在命令输出、日志或异常中打印值；
4. 如果文件不存在、权限不安全、变量为空或 API 返回认证失败，再停止并向用户报告；
5. 文档中原先“Token 未设置”的事实应在验证后更新为“实施时已安全配置并通过非泄漏验证”，不得写入值、长度、前后缀或哈希。

# 四、环境与统一入口

安装并锁定：

- uv；
- uv 管理的 CPython 3.12；
- Quarto；
- 项目所需 Python 依赖；
- 测试、Notebook 执行、静态站构建、链接检查和安全扫描工具。

不要使用系统 Python 3.14.4 作为项目运行时。

创建统一入口，至少支持：

- setup
- probe
- fetch
- test
- build
- preview
- audit
- all

可以使用 project.sh、Makefile 或等效入口，但 README 中只能有一套推荐命令。

要求：

- 每个命令失败时返回非零状态；
- 错误信息必须可行动；
- 不得输出 Token；
- 重复执行应安全；
- test 和 CI 不需要 Token；
- build 默认可使用冻结、合规的已有产物；
- fetch 才允许访问真实 Tushare 数据；
- all 必须按安全顺序执行。

# 五、先做 Tushare 能力探测

在全量下载前，对以下九个端点执行最小化探测：

- trade_cal；
- index_basic；
- index_weight；
- daily；
- adj_factor；
- daily_basic；
- stk_limit；
- suspend_d；
- index_daily。

每个探测只请求最小日期窗口和最少字段，并形成脱敏报告，记录：

- endpoint；
- 请求用途；
- 是否成功；
- API 状态或脱敏错误类别；
- 响应字段；
- 行数；
- 最早和最晚日期；
- 实际证券或指数代码；
- 是否满足 2018–2025；
- 是否满足设计用途；
- 实际权限；
- 选用的调用模式；
- 预计全量请求次数；
- 唯一降级方式。

禁止记录：

- Token；
- 请求头；
- Cookie；
- 可恢复认证信息；
- 大段原始响应。

默认全局速率不得超过 180 次/分钟。实现：

- 令牌桶或等效限流；
- 429 和暂时性失败退避；
- 权限及参数错误不盲目重试；
- 分片进度；
- 断点续跑；
- 成功响应不可变缓存；
- 内容哈希；
- 同一请求返回冲突时保留两个版本并触发数据门。

单个端点失败时按技术设计中的唯一回退处理，不要直接放弃全部真实数据路线。

# 六、基准探测

不得把裸 H00300 直接传给 API。

固定流程：

1. 从 index_basic 发现沪深300价格指数及可能的全收益指数完整代码；
2. 对候选代码执行最小 index_daily 探测；
3. 只有接口、权限、字段和历史覆盖均成功，才使用全收益指数；
4. 研究护照记录实际完整代码、来源、覆盖和是否包含股息；
5. 全收益指数不可用时，使用探测成功的沪深300价格指数；
6. 页面、图表和结果卡必须明确写“价格指数，不含股息”；
7. 相对价格指数的超额收益不得成为 REG 唯一硬门；
8. 不得虚构、拼接或猜测全收益数据。

# 七、真实数据与时点

真实 Tushare 数据是正式案例主路线。

教学数据只能用于：

- 单元测试；
- 契约坏样本；
- 无 Token 的 CI；
- 接口失败演示；
- 无 Token 产品预览。

不得用教学数据补齐正式结果，也不得把教学收益描述成市场证据。

统一字段：

- trade_date；
- effective_date；
- ingested_at；
- usable_from；
- signal_at；
- execution_at。

必须满足：usable_from <= signal_at < execution_at。

源接口没有提供的历史可用时间不得伪装成原始字段。

月末信号默认在所有 P0 月末数据进入保守可用窗口后的 18:00 形成，下一交易日开盘执行。

特别处理：

- index_weight 使用设计中的保守 as-of 规则，并完成滞后一月敏感性；
- suspend_d 是执行约束/事件证据，不是预测特征；
- 不得因为当前抓取时间晚于历史日期，就错误地把全部历史停牌记录判为不可用；
- 同时不得声称知道无法证明的历史发布时刻；
- 对这类限制在数据护照中标黄，并提供基于缺失 daily 的保守敏感性对照；
- daily_basic.limit_status 只用于收盘诊断；
- 涨跌停执行使用 stk_limit；
- 正式停牌证据优先使用 suspend_d。

# 八、研究实现

正式案例统一使用：

- A 股；
- 沪深300历史成分；
- 2018-01-01 至 2025-12-31；
- 探索期：2018-01-01 至 2022-12-31；
- 确认期：2023-01-01 至 2025-12-31；
- 日频输入；
- 月末信号；
- 下一交易日开盘执行；
- 月度调仓；
- 价值、12-1 动量、低波动三因子；
- 截面缺失处理、winsorize、市值中性化和标准化；
- 三因子等权复合；
- Top 20% 等权长仓；
- 成本、换手、延迟成交和基准比较；
- IC、ICIR、五组收益、单调性、衰减、子期稳定性；
- 风险暴露和容量限制说明；
- 样本外与稳健性分析。

收益必须通过原始价格和相邻复权因子计算：

r_t = (price_t × adj_factor_t) / (price_t-1 × adj_factor_t-1) - 1

不得依赖未来最终复权因子归一化的绝对价格水平。

统一并测试：

- 因子方向；
- 标签后移；
- open-to-open 或正式锁定的组合收益区间；
- 基准区间；
- 实际成交时点；
- 未成交持仓收益；
- 换手；
- 成本；
- 延迟订单；
- 公司行动处理。

成交状态机：

- 无有效日线或全天停牌：不成交；
- 买入且开盘达到涨停价：不成交；
- 卖出且开盘达到跌停价：不成交；
- 卖出涨停或买入跌停不自动视为不可成交；
- 其余具有有效开盘数据和成交量的情况按设计假设成交；
- 未成交保留原持仓并在后续有效交易日重试；
- 报告延迟次数、延迟天数、受影响权重、实际换手和成本；
- 明确披露日频模型无法模拟排队、盘口深度和部分成交。

# 九、确认集治理

首次查看确认期正式结果前，冻结：

- 假设；
- 因子定义；
- 参数；
- 数据快照；
- 股票池规则；
- 成交规则；
- 成本；
- 基准；
- 评价指标；
- REG 阈值。

证据护照记录：

- confirmation_reveal_count；
- research_decision_count；
- reproduction_run_count；
- contaminated；
- contamination_reason。

规则：

- 相同输入的确定性复现不算新研究决策；
- 工程修复重跑必须保留记录；
- 查看确认结果后修改研究设计，确认结果即被污染；
- 被污染结果只能标为探索性；
- 不得删除失败实验或重置记录伪装未查看；
- 不允许为了让确认期结果更好而自动调参；
- 可信的负结果是有效研究成果，不得美化为盈利策略。

# 十、失败实验与 REG

至少实现四个一次只改变一个错误的受控失败实验：

1. 使用未来数据或标签错位；
2. 用当前成分股回填历史；
3. 忽略交易成本；
4. 查看确认期后重新选参。

REG 输出两层判断。

研究有效性：D 数据、T 时序或 P 复现出现红灯时，研究结论无效。

策略推进价值：S 统计、C 成本或 R 稳健性不足时，输出“补证”或“停止”，不把项目判为失败。

每个门必须：

- 有输入；
- 有规则；
- 有证据；
- 有状态；
- 有解释；
- 有下一步；
- 能被正确案例和失败案例验证。

# 十一、内容产品

构建一个真正可使用的中文学习站，而不是代码展示页。

必须包含：

- 30 分钟速览；
- 3 小时新人路径；
- 深入实践路径；
- 20 步研究地图；
- 贯穿案例；
- 正确/失败对照；
- REG；
- 研究护照；
- 问题卡、假设卡、数据字典、实验记录、结果卡和评审模板；
- 前测、后测或章节练习；
- 术语表；
- 引用；
- 数据限制；
- 免责声明；
- 用户八步自测。

每一研究步骤至少说明：

- 为什么存在；
- 输入；
- 操作；
- 输出；
- 常见错误；
- 质量门；
- 与下一步的关系；
- 案例中的实际证据。

图表必须包含：

- 标题；
- 时间区间；
- 频率；
- 数据来源；
- 实验 ID；
- 成本口径；
- 基准口径；
- 文本解释；
- 必要的替代文字。

不要只展示漂亮曲线。需要展示证据链、失败原因和研究局限。

Notebook 不能承载唯一业务逻辑；核心计算必须位于可测试的 Python 包中。

# 十二、README 与使用体验

README 首屏必须包含：

- 一句话产品定义；
- GitHub Pages 入口；
- 三条学习路径；
- 最短本地启动方式；
- 有 Token 的真实数据刷新方式；
- 无 Token 的教学预览方式；
- 测试和构建命令；
- 数据权限与缓存说明；
- 不上传原始数据的说明；
- 项目截图；
- 免责声明。

新用户仅阅读 README 就应知道：

1. 去哪里开始；
2. 如何在线使用；
3. 如何离线使用；
4. 如何运行 Notebook；
5. 如何刷新真实数据；
6. 没有 Token 时能做什么；
7. 如何验证项目。

# 十三、测试与验证

至少实现并通过：

- 数据契约测试；
- 主键、范围和覆盖测试；
- usable_from 时序测试；
- 标签移位测试；
- 复权收益手算测试；
- 因子方向和排序测试；
- winsorize、标准化和中性化测试；
- IC 与分组测试；
- 组合权重测试；
- 成交状态机测试；
- 延迟订单测试；
- 换手和成本测试；
- 基准区间测试；
- 四个失败实验测试；
- REG 状态测试；
- 研究护照与确认污染测试；
- 缓存不可变和冲突测试；
- 限速、429、断点续跑测试；
- Notebook 端到端执行；
- Quarto 构建；
- 离线 HTML；
- 内部链接检查；
- 秘密扫描；
- README 冷启动检查。

测试覆盖率作为诊断信息，不以追求数字替代关键路径正确性。

真实数据结果不得设置“必须盈利”的测试。

# 十四、视觉与使用验证

完成站点后必须实际渲染并检查：

- 首页；
- 三条路径；
- 研究地图；
- 案例核心章节；
- 正确/失败对照；
- REG 页面；
- 模板；
- 移动或窄屏布局；
- 图表；
- Mermaid；
- 代码块；
- 中文字体；
- 链接；
- 页内导航。

发现截断、溢出、字体、对比度、表格或导航问题应修复后重新检查。

使用本地浏览器检查本地站点；部署后再检查公开 Pages URL。

# 十五、P1

P0 全部完成且无安全阻断后，继续实现有实际价值的 P1：

1. 轻量交互组件；
2. 行业中性敏感性；
3. 英文一页摘要；
4. 稳定 PDF。

规则：

- 不复制研究逻辑；
- 不允许在确认集任意调参；
- 不改变主假设；
- 不影响离线核心；
- PDF 失败不得破坏 HTML；
- 数据权限不支持行业中性时，明确跳过并记录原因。

# 十六、公开前安全与许可审计

推送前必须检查：

- tracked files；
- untracked files；
- Git 历史；
- Notebook 输入和输出；
- _site；
- PDF；
- 日志；
- 测试快照；
- 缓存 manifest；
- 错误信息；
- README；
- AI_PROMPTS.md；
- .env*；
- 配置文件；
- 高熵字符串；
- Token/API Key/私钥模式；
- 本机用户名；
- 私有绝对路径；
- Tushare 原始响应；
- 可大规模还原付费数据的文件。

允许公开：

- 源代码；
- 原创学习内容；
- 自制图表；
- 不可逆汇总指标；
- 确定性教学夹具；
- 哈希；
- 字段定义；
- 复现说明。

默认不公开：

- Token；
- 私钥；
- Cookie；
- .env；
- Tushare 原始缓存；
- 可重构原始数据的导出；
- 带秘密的历史；
- 授权不明的原始数据。

如果发现 Token 已进入 Git 历史：

1. 不得推送；
2. 停止发布；
3. 报告用户需要轮换 Token；
4. 清理历史；
5. 全量复扫；
6. 通过后才能继续。

# 十七、提交、推送与 Pages

只有在以下条件全部满足后才能提交和推送：

- P0 完成；
- 关键测试通过；
- Notebook E2E 通过；
- Quarto 构建通过；
- 离线 HTML 可用；
- 正确和失败实验可复现；
- REG 正确；
- 文档一致；
- AI_PROMPTS 完整；
- 安全扫描无阻断项；
- 许可检查无阻断项；
- 不含原始授权数据。

完成后：

1. 检查 Git diff；
2. 创建清晰的提交；
3. 推送当前关联的 GitHub 仓库；
4. 配置或触发 GitHub Pages；
5. 等待部署完成；
6. 打开公开 URL；
7. 检查主要页面和资源；
8. 记录最终 URL、提交 SHA、构建状态和审计结果。

如果 GitHub 认证或 Pages 权限确实需要用户操作，再准确报告唯一阻塞动作。不要以此为理由跳过本地完成和验证。

# 十八、用户自测

交付前准备八步自测说明：

1. 从 GitHub README 进入；
2. 打开在线学习站；
3. 完成 30 分钟路线；
4. 查看或运行贯穿案例；
5. 找出失败案例中的错误；
6. 用 REG 判断继续、补证或停止；
7. 用模板提出一个新研究问题；
8. 判断该产品是否能直接用于新人培训并记录问题。

Agent 自己先完整走一遍，修复明显问题。最终再请用户亲自试用。

# 十九、执行方式

- 不要再次输出大段计划后停止；
- 读完文档后立即开始；
- 可以并行处理彼此独立的任务，但避免多个执行单元同时修改同一文件；
- 每完成一个阶段就运行对应测试；
- 遇到真实数据不支持预期结论时，保留真实结果并解释；
- 不得为了好看的收益曲线偷偷调参；
- 不得删除失败记录；
- 不得覆盖用户已有工作；
- 不得用“示例已完成”代替真实功能；
- 不得声称完成尚未运行的测试或部署；
- 持续工作直到交付完成或遇到只有用户才能解决的真实阻塞。

# 二十、最终回复

最终只汇报经过验证的事实：

1. 在线学习站 URL；
2. GitHub 仓库和最终提交 SHA；
3. 本地运行命令；
4. 测试、Notebook、构建、链接和安全扫描结果；
5. Tushare 实际可用端点；
6. 实际使用的基准代码和是否包含股息；
7. 正式案例的研究结论；
8. REG 结论；
9. 正确与失败实验；
10. P1 完成情况；
11. 数据和模型局限；
12. 用户八步自测入口；
13. AI_PROMPTS 是否完整；
14. 尚存的非阻塞问题。

不要输出 Token、原始授权数据或无法验证的完成声明。
~~~~

## AP-007

- 所属阶段：正式实施（并行工作包）
- 提问对象：Codex 子执行单元
- 提示词原文：

~~~~text
阅读 REQUIREMENTS.md 与 docs/00–06。负责实现数据访问工作包，文件边界仅限 src/quant_onboarding/data.py、src/quant_onboarding/probe.py、tests/test_data.py、tests/test_probe.py；如目录不存在可创建。实现安全 Token 加载（不输出值）、≤180/分钟限流、429/暂时失败退避、权限错误不盲重试、仓库外不可变内容寻址缓存、冲突保留与报错、进度/断点续跑、脱敏 manifest、九端点最小探测及基准候选发现。测试必须完全使用 mock/夹具，不访问网络。不要修改 REQUIREMENTS.md、AI_PROMPTS.md、README、配置、其他 src/tests 文件，不提交 Git。完成后运行你负责的测试并报告。
~~~~

## AP-008

- 所属阶段：正式实施（并行工作包）
- 提问对象：Codex 子执行单元
- 提示词原文：

~~~~text
阅读 REQUIREMENTS.md 与 docs/00–06。负责实现核心量化研究工作包，文件边界仅限 src/quant_onboarding/research.py、src/quant_onboarding/execution.py、src/quant_onboarding/reg.py、tests/test_research.py、tests/test_execution.py、tests/test_reg.py；如目录不存在可创建。实现相邻复权收益、标签后移、winsorize、标准化、市值中性化、价值/12-1动量/低波动、IC/ICIR/五组收益/单调性/衰减/分期、Top20%等权组合、保守成交状态机、延迟订单/换手/成本、四个受控失败实验、六门REG与确认污染护照。全部使用确定性小型数据测试，不访问真实 API。不要修改 REQUIREMENTS.md、AI_PROMPTS.md、README、配置、其他 src/tests 文件，不提交 Git。完成后运行你负责的测试并报告。
~~~~

## AP-009

- 所属阶段：正式实施（并行工作包）
- 提问对象：Codex 子执行单元
- 提示词原文：

~~~~text
阅读 REQUIREMENTS.md 与 docs/00–06。负责中文学习产品内容工作包，文件边界仅限 content/、templates/、assets/、README.md。创建可直接用于 Quarto 的首页、30分钟、3小时、深入路径、20步研究地图、贯穿案例、正确/失败对照、REG、研究护照、模板、术语表、引用、数据限制、免责声明、八步自测和英文一页摘要（P1）；每一步说明为什么、输入、操作、输出、错误、质量门、下一步、案例证据。加入可访问的视觉样式与站点截图占位方案，但不要伪造真实市场结论。README 首屏满足冻结合同，推荐统一命令 scripts/project.sh。不要修改 REQUIREMENTS.md、AI_PROMPTS.md、docs/、配置、src/tests，不提交 Git。完成后检查内部链接和内容结构并报告。
~~~~

---

## 用户提示词（实施阶段）

现在开始正式实施本项目。

你的任务不是继续讨论方案，而是严格依据现有需求与设计完成产品、验证、公开发布和最终交付。持续推进直到 P0 完整可用、测试通过、公开仓库安全、GitHub Pages 可访问；不要停留在骨架、示例或阶段性方案。

[完整提示词已记录，包含：项目依据、用户授权、安全动作、环境与统一入口、Tushare能力探测、基准探测、真实数据与时点、研究实现、确认集治理、失败实验与REG、内容产品、README与使用体验、测试与验证、视觉与使用验证、P1、公开前安全与许可审计、提交推送与Pages、用户自测、执行方式、最终回复]

## AP-010

- 所属阶段：正式实施续接
- 提问对象：Codex（本项目助手）
- 提示词原文：

~~~~text
继续
~~~~

## AP-011

- 所属阶段：正式实施续接
- 提问对象：Codex（本项目助手）
- 提示词原文：

~~~~text
由于额度，发生了中断。我完成了环境依赖安装，完成了数据拉取。帮我继续推进项目到可以交付的水平
~~~~
## AP-012

```text
现在做了多少了，还有哪些没做的
```
## AP-013

```text
是否有适合deepseek（没有视觉能力）做的工作，给我明确的prompt。
```
## AP-014

```text
先直接同步发布一版，后面再补上问题修复
```
## AP-015

```text
无需重复校验，直接发布
```

## AP-016

```text
已启用
```
